https://github.com/Javed0011/Rest-Assured-Hybrid-Framework.git
-------------------------------------------------------------------------------------------------------------------------------  
  Git Commands
-------------------------------------------------------------------------------------------------------------------------------

1. git init - creates empty local repository

First Configuration
{
2. git config --global user.name "Javed Nadaf"
3. git config --global user.email "javednadaf072@gmail.com"
}

4.git status - displays the files which need to be added to index (staging)
5.git add -A --adds all the files to the staging
  git add filename.java -- used to add a particular file

6.git status 
7.git commit -m "First Commit"

8.git remote add origin "URL" -- establish connection between local and GitHub

9.git push -u origin master